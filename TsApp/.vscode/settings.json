// Place your settings in this file to overwrite default and user settings.
{
    //   "files.exclude": {
    //        "**/*.js": { "when": "$(basename).ts"},
    // 	    "**/*.js.map": {}
    //     }
    // *.d.ts, *.js, *.map, **/node_modules, **/app/components
    "files.exclude": {
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.js.map": {},
        "**/.git": true,
        ".gitignore": true,
        "**/.DS_Store": true,
        "**/node_modules": true,
        "**/app/components": true,
        "*.map": true,
        "**/typings": true
    }
}